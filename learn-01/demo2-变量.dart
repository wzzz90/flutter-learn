// Dart 变量：
// dart是一个强大的脚本类语言，可以不预先定义变量类型，自动类型推断。
// dart中定义变量可以通过var关键字，也可以通过类型来声明变量



main() {
  // dart和js一样，会自动类型推断。
  // 变量命名，可以使用var或者类型
  // var str = 'hello world';
  // var myNum = 123;
  // print(str);
  // print(myNum);


  // 字符串声明

  // String str = 'string声明';

  // print(str);


  // //数字类型

  // int myNum = 1234;

  // print(myNum);


// dart中存在类型校验
//  var在定义时进行类型推断，之后重新修改变量类型的话，会报错
  // var str = '';
  // str = 123;

  // 当生命的变量类型与输入的不一致的时候，会报错
  // String str = 123;
  // print(str);
}

//表示该main方法没有返回值。
// void main() {
//   print('object');
// }

